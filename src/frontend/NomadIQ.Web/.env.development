# Local development defaults - matches each backend service's launchSettings.json.
# No secrets here; safe to commit. Override locally via .env.local if needed.

VITE_IDENTITY_API_BASE_URL=http://localhost:5000
VITE_TRIP_API_BASE_URL=http://localhost:5020
VITE_DISCOVERY_API_BASE_URL=http://localhost:5296
VITE_AI_API_BASE_URL=http://localhost:5041
